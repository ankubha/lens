# Lens — Unified Document & Data Intelligence Platform

A platform that profiles structured (CSV/XLSX), semi-structured (JSON/XML),
and unstructured (PDF/DOCX) data through a single intelligence layer.